global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # --- P4 (Observability phase) adds real scrape targets here, e.g.: ---
  # - job_name: 'kafka-exporter'
  #   static_configs:
  #     - targets: ['kafka-exporter:9308']
  # - job_name: 'cassandra-exporter'
  #   static_configs:
  #     - targets: ['cassandra-exporter:7070']
  # - job_name: 'node-exporter'
  #   static_configs:
  #     - targets: ['node-exporter:9100']
